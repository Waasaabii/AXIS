# ProxyRelay 需求说明

## 一句话概述

构建一个部署在服务器上的代理中转服务，由服务器负责订阅拉取、节点解析、
出口组织和运行态控制，再统一向客户端暴露标准的 SOCKS5 入口。


## 项目目标

项目解决的问题不是“让客户端继续直接导入机场订阅”，而是：

- 把订阅消费能力收敛到服务器端
- 把机场节点抽象成稳定的对外 SOCKS5 入口
- 用控制面统一管理订阅、出口组、监听端口和运行状态
- 降低客户端复杂度，提高服务端运维可控性


## 当前阶段

截至 2026-03-20，项目已经完成：

- **V0.5 Ubuntu 联调版主线**
- **多页面后台重构**

当前现实状态如下：

- 控制面实现：`Node.js`
- 目标数据面：`mihomo`
- 已支持两类运行方式：
  - 仓库内默认配置：`config/proxyrelay.yaml`，偏本地 render-only / 本地联调
  - 系统级配置：`/etc/proxyrelay/proxyrelay.yaml`，偏 Ubuntu managed 运行态
- 当前前端形态：**多页面后台**
- 已有本地启动脚本：`deploy/start-local.sh`
- 已有 Ubuntu 安装脚本：`deploy/install-ubuntu.sh`

这一阶段的重点已经不再是“能不能起服务”，而是：

- 用真实订阅验证真实 provider / group / listener 链路
- 继续提升后台易用性
- 把运维和部署能力收口


## 功能范围

### 1. 控制面

系统需要提供一套轻量控制面，用来管理配置、查看状态和触发运行态动作。

最少应支持：

- 单用户后台登录
- 配置读取与保存
- 总览状态读取
- provider、group、listener、events 读取
- 运行态重载
- controller 探测
- 运行预检


### 2. 订阅与节点

系统需要支持在服务器端处理远程订阅，而不是把订阅直接下发给客户端。

应支持：

- 配置远程订阅地址
- 手动刷新订阅
- 定时刷新订阅
- 解析节点并输出预览
- 展示节点基础元信息

MVP 已支持的订阅预览协议：

- VMess
- VLESS
- Shadowsocks
- Clash YAML 中的 `proxies`


### 3. 出口组

对外端口不直接永久绑定某个原始节点，而是绑定逻辑出口组。

应支持：

- 单 provider 下按规则筛选候选节点
- 手动组
- 自动组
- 基于组执行切换
- 基于组执行健康检查


### 4. 对外接口

系统对客户端暴露的核心能力是 SOCKS5 监听端口。

应支持：

- 同时暴露多个 SOCKS5 端口
- 每个端口绑定不同出口组
- 用户名 / 密码认证
- 查看每个端口的监听地址、端口、认证用户数和当前出口映射


### 5. 系统配置与渲染

控制面应负责把高层配置转换成运行态可读的 `mihomo.yaml`。

应支持：

- 读取 `proxyrelay.yaml`
- 校验订阅、出口组、监听器配置
- 渲染出 `mihomo.yaml`
- 保留 `mihomo.last-good.yaml`
- 记录运行态状态文件
- 在 managed 模式下尝试把最新配置下发到运行中的 controller


### 6. 状态与预检

系统需要提供基础的运维可观测性。

应支持：

- 总览状态
- controller 状态
- 最近事件
- Ubuntu 运行预检

当前预检已覆盖：

- runtime 路径是否对齐
- runtime / providers 是否可写
- 核心渲染产物是否存在
- `mihomo` 二进制是否检测到
- controller secret 是否配置
- controller 是否可达
- 最近一次运行态下发状态
- systemd unit 文件是否就位


## 前端需求

### 当前实现目标

后台不再继续走“一个页面堆所有功能”的方向，而是按功能域拆成多页面。

当前页面划分应为：

- `dashboard.html`：总览
- `status.html`：状态
- `subscriptions.html`：订阅配置
- `interfaces.html`：对外接口配置
- `system.html`：系统配置
- `events.html`：事件日志

页面设计要求：

- 左侧固定主导航
- 页面之间是真实跳转，不是页内锚点
- 整体使用灰白主调
- 使用正常、克制的系统字体
- 每页只承载一个主要任务域，避免堆叠


### 后续前端目标

当前后台已经多页面化，但仍缺以下能力：

- 结构化 CRUD 表单
- 更明确的表格式配置编辑
- 更强的页面级状态提示
- 更细的错误边界和运维提示


## 非功能要求

### 可用性

- 控制面应长期稳定运行
- 订阅更新失败不应立即破坏当前已生效配置
- 单个节点失效不应导致整个服务完全不可用


### 安全性

- SOCKS5 对外端口必须支持认证
- 不直接向客户端暴露机场订阅
- 敏感配置需要安全存储
- 控制面默认应以内网监听为主，公网暴露需谨慎


### 可维护性

- 配置结构要清晰
- 日志与状态要足够可读
- 文档与实现要保持同步
- 支持本地启动、系统级部署和手工联调三种使用方式


### 可扩展性

- 后续支持更多协议
- 后续支持更多出口策略
- 后续支持指标、告警和更强的运维能力


## 当前已完成范围

### 已完成

- 单用户登录
- 配置读写
- 订阅测试与节点预览
- `mihomo.yaml` 渲染
- controller 适配层
- Ubuntu 运行预检
- Ubuntu 安装脚本
- systemd 模板
- 多页面后台
- 本地启动脚本
- managed 模式下的运行态 reload
- Ubuntu 实机部署和 controller 验证


### 当前未完成

- 真实订阅替换和真实 provider 验证
- 真实 SOCKS5 出口链路验证
- 结构化 CRUD 表单
- 更完整的 systemd / 端口 / 订阅联通性预检
- 运行日志、指标和生产化能力


## 分阶段目标

### V0.3 控制面收口版

已完成。

目标是把控制面、登录、配置、渲染和基础后台搭起来。


### V0.5 Ubuntu 联调版

主线已完成。

目标包括：

1. 接入真实 `mihomo`
2. 对接 `external-controller`
3. 验证 managed 模式 reload
4. 跑通 Ubuntu 预检和 systemd 基础能力


### 当前阶段：V0.5 收口后的后台与联调整理

当前实际重点：

1. 用真实订阅验证 provider / group / listener 链路
2. 继续细化多页面后台
3. 补结构化配置管理
4. 扩展运维与部署收口


### V1.0 可用版

后续目标包括：

1. 自动健康检查
2. 自动选优和故障切换
3. 更完整日志和指标
4. 更完善的部署和回滚策略
5. 更强的生产环境安全加固


## 非目标

当前阶段暂不追求：

- 多租户 / 多用户系统
- 自研代理协议内核
- 复杂分流规则系统
- 高可用集群编排
- 一开始就做成完整 SaaS 平台
