# AGENTS

## AXIS 产品模型 Skill

- 开发 AXIS 任意前端、后端、配置、API、文案、线路编排、发布能力、虚拟网口或节点来源相关内容前，必须先读取并遵守 `.skills/axis-product-model/SKILL.md`。
- 该 Skill 是 AXIS 的核心产品概念来源；详细概念、页面边界、信息架构、文案规则与数据模型约束均以该 Skill 及其 `references/` 文档为准。

AXIS 的后端架构调整只遵守这一条硬约束：

- 代码必须优先保证可复用、可收敛、可约束；同一语义只能有一个归属点，禁止在多个位置重复实现同一套业务规则、配置读写、运行时逻辑或响应结构。
- 必须通过清晰的分层与边界限制大模型随意扩散实现；传输层、业务层、基础设施层职责必须单一，禁止跨层直接复制逻辑或绕过既有归属点新增“第二套实现”。
